<%= form_for @story do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <!-- CH & V-$ posit: Although the image input still exists, it doesnt appear to be used anywhere now that the images are all icons in assets folder -->
  <%= f.label :image %>
  <%= f.text_field :image %>
  <%= f.submit %>
<% end %>
